# ---------------------------------------
# Fenn Configuration (Modify Carefully)
# ---------------------------------------

project: vae-mnist

logger:
  dir: logger

# ---------------------------------------
# User Section
# ---------------------------------------
general:
  device: cuda

model:
  hidden_dim: 400
  z_dim: 20

train:
  seed: 42
  batch: 128
  epochs: 30
  lr: 1e-3
  beta: 1.0

test:
  batch: 128

dataset:
  dir: outputs/data
